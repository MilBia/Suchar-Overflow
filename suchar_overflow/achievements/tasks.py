# Periodic achievement awards are handled by the management command
# `award_periodic --period month|year`, scheduled via cron.
# See compose/*/django/start-cron and award_periodic.py management command.
